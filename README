Simple Android app showing the usage of the JNI to perform superuser calls that do not block the main thread.

Read the comments in MainActivity.java.

Also features a very aggressive proguard config.
